import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('QOR для упаковки и аксессуаров', 'Позиция без срока: риск списания не считается, а точка заказа считается.', '/solutions/packaging');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
