import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('QOR для сети цветочных', 'Сначала перемещение излишка между точками, потом закупка.', '/solutions/chain');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
