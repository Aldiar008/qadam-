import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('QADAM для кофейни', 'Заполняйте тихие часы и возвращайте постоянных гостей.', '/solutions/cafe');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
