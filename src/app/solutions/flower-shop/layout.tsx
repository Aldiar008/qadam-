import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('QOR для цветочного магазина', 'Остаток партиями со сроком: видно и сколько стеблей, и сколько дней им осталось.', '/solutions/flower-shop');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
