import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('QADAM для сервисного центра', 'Возвращайте клиентов на своевременное обслуживание.', '/solutions/service-center');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
