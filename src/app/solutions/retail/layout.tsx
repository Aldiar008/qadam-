import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('QADAM для магазина', 'Персональные предложения для роста среднего чека и возврата.', '/solutions/retail');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
