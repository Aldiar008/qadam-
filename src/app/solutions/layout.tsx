import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('Решения для бизнеса', 'Сценарии роста QADAM для разных форматов локального бизнеса.', '/solutions');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
