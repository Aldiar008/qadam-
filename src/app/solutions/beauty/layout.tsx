import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('QADAM для салона красоты', 'Управляйте повторными визитами и свободными слотами.', '/solutions/beauty');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
