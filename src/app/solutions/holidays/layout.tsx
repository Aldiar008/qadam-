import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('QOR к восьмому марта и выпускным', 'Повод попадает в прогноз заранее — с коэффициентом, регионом и пометкой, что это гипотеза.', '/solutions/holidays');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
