import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('Impact Ledger', 'Forecast, demo results и verified facts в прозрачной аналитике.', '/features/analytics');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
