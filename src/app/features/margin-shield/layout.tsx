import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('Margin Shield', 'AI-защита от финансово опасных скидок и убыточных акций.', '/features/margin-shield');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
