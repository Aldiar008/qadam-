import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('Прогноз спроса', 'История продаж плюс события Казахстана: Наурыз, жара, начало учебного года.', '/features/local-pulse');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
