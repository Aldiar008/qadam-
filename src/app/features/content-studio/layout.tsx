import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('Контент-студия', 'RU и KK контент для WhatsApp, Telegram, постов и Stories.', '/features/content-studio');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
