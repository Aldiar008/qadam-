import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('AI-кампании', 'Персональные механики роста для конкретной цели и аудитории.', '/features/ai-campaigns');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
