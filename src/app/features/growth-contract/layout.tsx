import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('Growth Contract', 'Прозрачная рекомендация с аудиторией, экономикой и stop-rule.', '/features/growth-contract');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
