import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('Доказанный эффект', 'Прогноз, влияние и подтверждённый факт лежат отдельно и не смешиваются.', '/features/impact-ledger');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
