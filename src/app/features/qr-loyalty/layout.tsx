import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('QR-лояльность', 'Mini-CRM и согласие клиента через один QR-код на кассе.', '/features/qr-loyalty');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
