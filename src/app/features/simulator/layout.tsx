import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('Симулятор акции', 'Проверьте экономику предложения до запуска кампании.', '/features/simulator');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
