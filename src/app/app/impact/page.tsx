import { PlannedSection } from '@/components/app/PlannedSection';

export const dynamic = 'force-dynamic';

export default function ImpactPage() {
  return (
    <PlannedSection
      title="Эффект"
      summary="Что снабжение дало в деньгах и времени — с честным разделением прогноза, наблюдаемой связи и подтверждённого факта."
      willShow={[
        'Три раздела: прогноз, влияние и подтверждённый факт — они не суммируются между собой',
        'Прогноз экономии от автозаказа и сумма, замороженная в излишках',
        'Сколько времени владельца ушло бы на сбор тех же заказов вручную',
        'Формула, период и источник для каждого числа',
        'Подтверждённый эффект равен нулю, пока не проведён пилот с базовым периодом',
      ]}
      dataSource="Каждая запись хранит своё состояние доказанности и не может перейти в «подтверждено» без новых данных — только по времени этого не происходит. Обычный пользователь не может присвоить записи состояние подтверждённого факта."
      outOfScope="Здесь не будет одной крупной цифры «вы сэкономили столько-то»: она была бы неотличима от прогноза, а значит бесполезна."
      fallback={{ label: 'Пока смотреть аналитику', href: '/app/analytics' }}
    />
  );
}
