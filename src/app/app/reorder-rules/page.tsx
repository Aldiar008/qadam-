import { PlannedSection } from '@/components/app/PlannedSection';

export const dynamic = 'force-dynamic';

export default function ReorderRulesPage() {
  return (
    <PlannedSection
      title="Правила автозаказа"
      summary="Когда система сама собирает черновик заказа: по какому порогу срабатывает, сколько считает и в каких границах остаётся."
      willShow={[
        'Правила по позициям и категориям с условием срабатывания',
        'Точка перезаказа и страховой запас — заданные вручную или подставленные по профилю',
        'Границы: недельный бюджет, максимальное количество, запрет на нового поставщика',
        'Включение и отключение правила без потери истории',
        'Что правило сделало за последние дни и почему промолчало',
      ]}
      dataSource="Правило смотрит на остаток, дневной прогноз расхода и срок поставки. При срабатывании оно создаёт черновик решения, а не заказ: деньги и обязательства остаются за человеком."
      outOfScope="Правило никогда не отправляет заказ поставщику само — отправка требует отдельного подтверждения владельца."
      fallback={{ label: 'Пока смотреть автоматизации', href: '/app/automations' }}
    />
  );
}
