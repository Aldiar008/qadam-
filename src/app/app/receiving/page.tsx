import { PlannedSection } from '@/components/app/PlannedSection';

export const dynamic = 'force-dynamic';

export default function ReceivingPage() {
  return (
    <PlannedSection
      title="Приёмка"
      summary="Шаг, который замыкает цикл: сколько заказали против того, сколько реально привезли и когда."
      willShow={[
        'Список ожидаемых поставок на сегодня и ближайшие дни',
        'Ввод фактического количества по каждой позиции',
        'Отметка опоздания и причины расхождения',
        'Автоматическое обновление остатка после приёмки',
        'Пересчёт надёжности поставщика с показом «было → стало»',
      ]}
      dataSource="Приёмка сверяет факт с заказом одной транзакцией: расхождение создаётся, если принято не то количество, остаток обновляется, а рейтинг поставщика пересчитывается по новой выборке. Принять одну поставку дважды нельзя."
      outOfScope="Претензионная работа и возвраты поставщику остаются вне системы — здесь фиксируется факт, а не спор о нём."
      fallback={{ label: 'Пока смотреть историю действий', href: '/app/journal' }}
    />
  );
}
