import { createPageMetadata } from '@/lib/metadata';
export const metadata = createPageMetadata('О QADAM', 'Почему локальный бизнес выигрывает отношениями и как QADAM усиливает это преимущество.', '/about');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
