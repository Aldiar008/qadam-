# SCREEN_MAP — целевая карта экранов QOR Autopilot

Дата: 14 августа 2026. Составлено против фактических маршрутов из
[PROJECT_STATE.md](PROJECT_STATE.md).

Столбец «Сейчас» описывает, что лежит по этому адресу в репозитории на
момент аудита, а не то, что запланировано.

---

## 1. Публичный контур

| # | Экран | Маршрут | Сейчас | Что нужно |
|---|---|---|---|---|
| 1 | Landing | `/` | **готов**: 14 секций под снабжение, дизайн и движение прежние | цифры сверить с демо-данными, когда они появятся |
| 2 | Каталог модулей | `/features` + 9 подстраниц | **готов** | — |
| 3 | Отраслевые профили | `/solutions` + `cafe`, `retail`, `bakery`, `pharmacy` | **готов** | — |
| 4 | Платформа, тарифы, о нас, контакты | `/platform`, `/pricing`, `/about`, `/contact` | **готов** | — |
| 5 | Демо-вход | `/demo` | **готов**, тексты обновлены | кнопка входа требует поднятой базы |
| 6 | Вход и регистрация | `/login`, `/signup`, `/forgot-password`, `/reset-password` | **работают** | не трогать |

## 2. Onboarding

| # | Экран | Маршрут | Сейчас | Что нужно |
|---|---|---|---|---|
| 7 | Профиль бизнеса | `/onboarding` | работает, вопросы маркетинговые | тип бизнеса, число точек, категории товаров; подстановка порогов и шаблонов поставщиков по профилю |

Требование кейса: «кофейне — автозаказ молока и стаканов по среднему
дневному расходу; мини-маркету — сезонный прогноз спроса на напитки летом».
Механика подстановки уже есть в `domain/tool-recommendations.ts`.

## 3. Кабинет — основной поток

Порядок в меню повторяет цикл: увидеть риск → решить → заказать → принять
→ проверить поставщика.

| # | Экран | Маршрут | Сейчас | Что нужно |
|---|---|---|---|---|
| 8 | **Quiet Morning Queue** | `/app/today` | работает, показывает маркетинговый сигнал и кампании | заменить содержимое: 3–5 решений на сегодня, отсортированных по срочности и сумме под риском |
| 9 | **Decision Contract** | `/app/decisions` + `/app/decisions/[id]` | каркас с описанием | главный экран продукта: риск, количество, поставщик, доказательства, отклонённая альтернатива, цена бездействия, подтверждение с причиной при изменении |
| 10 | Inventory & Policies | `/app/inventory` | каркас | остатки по точкам, точка перезаказа, страховой запас, упаковка, минимальная партия, срок годности; ввод приёмки, расхода, списания, перемещения |
| 11 | Прогноз спроса | `/app/forecast` | каркас | график 28 дней и прогноз вперёд, ошибка на бэктесте, календарь событий, метка «гипотеза» |
| 12 | Supplier Compare | `/app/suppliers` | каркас | карточка поставщика, доля поставок вовремя и полностью, размер выборки, личный и общий рейтинг раздельно |
| 13 | Цены поставщиков | `/app/supply` | **работает с данными** | достроить срок поставки и надёжность; впоследствии слить с экраном 12 |
| 14 | Purchase Orders | `/app/orders` | каркас | заказы со статусами `draft → sent → confirmed → in_transit → delivered/failed`, позиции, сообщение поставщику |
| 15 | Receiving | `/app/receiving` | каркас | факт против заказа, расхождение, опоздание, обновление остатка, пересчёт надёжности |
| 16 | Messenger Stock | `/app/messenger-stock` | каркас | входящее сообщение → разбор → подтверждение → событие остатка |
| 17 | Правила автозаказа | `/app/reorder-rules` | каркас | порог, расчёт количества, границы бюджета |
| 18 | Эффект | `/app/impact` | каркас | прогноз, влияние и подтверждённый факт раздельно; в демо verified = 0 ₸ |
| 19 | KPI снабжения | `/app/analytics` | работает, метрики маркетинговые | позиции на исходе, среднее время доставки, доля срывов, замороженные деньги |

## 4. Кабинет — сервисные разделы

| # | Экран | Маршрут | Сейчас | Что нужно |
|---|---|---|---|---|
| 20 | Tools Catalog | `/app/tools` | **работает с данными** | заменить содержимое карточек на пять инструментов снабжения; фильтры и избранное уже есть |
| 21 | Уведомления | `/app/notifications` | **работает** | добавить типы событий о поставках |
| 22 | История действий | `/app/journal` | **работает** | само наполнится новыми действиями |
| 23 | Команда | `/app/team`, `/app/team/accept` | **работает** | не трогать |
| 24 | Тариф | `/app/plan` | **работает** | тексты уже обновлены |
| 25 | Настройки | `/app/settings` | **работает** | убрать блок лояльности |

## 5. Админка

| # | Экран | Маршрут | Сейчас | Что нужно |
|---|---|---|---|---|
| 26 | Обзор | `/admin` | **работает** | метрики использования под снабжение |
| 27 | Категории товаров | `/admin/categories` | **работает** | заменить содержимое |
| 28 | Шаблоны поставщиков | `/admin/templates` | **работает** | заменить содержимое |
| 29 | Правила автозаказа | `/admin/rules` | каркас | CRUD правил |
| 30 | Типы бизнеса | `/admin/business-types` | каркас | CRUD профилей и наборов инструментов |
| 31 | Инструменты | `/admin/tools` | **работает** | заменить содержимое |
| 32 | Аналитика платформы | `/admin/analytics` | **работает** | активные бизнесы, популярные категории, средняя экономия |

## 6. Демо-режим и раскрытие

| # | Экран | Маршрут | Сейчас | Что нужно |
|---|---|---|---|---|
| 33 | Demo Reset / disclosure | нет отдельного маршрута | разделение demo/production живёт в БД, бейдж есть в интерфейсе | отдельная страница: что синтетика, что расчёт, что интеграция; кнопка сброса демо к исходному seed |

Это единственный экран целевой карты, которого сейчас нет вообще. Он
закрывает требование кейса о честной границе mock/real и снимает риск
«mock выглядит как настоящая интеграция».

## 7. Legacy — работает, но из меню не ведёт

`/app/campaigns` (+`new`, `studio`, `[id]`), `/app/content`, `/app/customers`,
`/app/segments`, `/app/loyalty`, `/app/inbox`, `/app/automations`,
`/app/recommendations`, `/customers/*`, `/nearby/*`, `/q/[token]`, `/tg/*`.

Все доступны по прямому адресу, RLS и тесты не тронуты.

---

## 8. Что из карты уже сделано

- Публичный контур: пункты 1–6 закрыты.
- Каркасы с описанием будущего экрана: пункты 9, 10, 11, 12, 14, 15, 16, 17, 18, 29, 30.
- Работают с живыми данными: 13, 20, 21, 22, 23, 24, 25, 26, 27, 28, 31, 32.
- Требуют переделки содержимого: 7, 8, 19.
- Не существует: 33.
