# HANDOFF — QADAM Growth OS

Передача проекта следующему агенту. Документ описывает три вещи: что уже
работает, на чём легко потерять время, и что предстоит сделать.

Дата передачи: 2026-08-03.

---

## 1. Что это за проект

AI-маркетолог для малого офлайн-бизнеса в Казахстане. Next.js 16 (App Router,
server actions) + Supabase (Postgres 17, RLS на всех тенантных таблицах).
46 миграций, 80 таблиц. Гость видит свою карту в Telegram Mini App (`/tg`),
владелец — «Сегодня» и подтверждение запуска там же.

Главный принцип продукта — **честность**: демонстрационные данные помечены
`is_mock` и никогда не выдаются за проверенный факт; канал не может стать
`connected` без пройденной проверки связи; запуск без подключённого провайдера
отклоняется, а не имитируется. Эти гарантии держатся не соглашениями, а
констрейнтами и триггерами в базе — ломать их нельзя, и обходить их в коде
приложения бессмысленно, база всё равно откажет.

Обязательное чтение перед первым изменением:
[PRODUCT_CONSTITUTION](PRODUCT_CONSTITUTION.md), [ARCHITECTURE](ARCHITECTURE.md),
[REAL_VS_MOCK](REAL_VS_MOCK.md), [KNOWN_LIMITATIONS](KNOWN_LIMITATIONS.md).

---

## 2. Что уже развёрнуто

**Один стенд.** Демо и боевой режим живут в одном приложении и одной базе.

| Что | Значение |
|---|---|
| Основной адрес | https://qadam-growth-os.vercel.app |
| Telegram-приложение гостя | https://qadam-growth-os.vercel.app/tg — открывается только из бота [@qadam_serpin_bot](https://t.me/qadam_serpin_bot) |
| Прежний адрес (жив, тот же проект) | https://qadam-demo.vercel.app |
| Vercel-проект | `qadam-demo` в организации `zhenups-2921s-projects` |
| Supabase | `lwqoclhgdvpydaetpypb`, регион `ap-northeast-1` |
| Свободный проект-эталон | `xeawgjbywhubnkuxtgho` — чистая схема без демо-данных |

- Функции Vercel закреплены в `hnd1` (Токио) рядом с базой. По умолчанию Vercel
  ставит их в `iad1`, и каждый запрос к базе шёл через Тихий океан.
- 27 миграций, 0 таблиц без RLS, гранты совпадают с ожиданием DEPLOY_RUNBOOK B4.
  Роль `authenticated` с таймаутами `statement_timeout=8s`,
  `idle_in_transaction_session_timeout=15s`.
- Данные: TAMYR Coffee (`mode='demo'`, 180 клиентов, 1129 транзакций, всё
  `is_mock`) и Tenant B Test Cafe (`mode='production'`, пусто). Второе — не
  избыточность: на этой паре `npm run test:modes` доказывает, что одна сборка
  обслуживает оба рода заведений по-разному.
- AI: `QADAM_AI_PROVIDER=gemini`, модель `gemini-3.6-flash`. Работает
  по-настоящему и в трёх местах: механики акций в студии, комплект контента и
  досье гостя. Каждый прогон пишется в `ai_generation_runs` с моделью,
  стоимостью и доказательствами проверок; при недоступности провайдера остаётся
  детерминированный шаблон, помеченный как шаблон.
- Автоматика GitHub Actions: CI на каждый push, Deploy после успешного CI,
  Execution cycle каждые пять минут.

### Результаты против развёрнутого стенда

| Набор | Результат |
|---|---|
| E2E владелец / клиент / администратор / Telegram-приложение | 58/58, 36/36, 45/45, 22/22 |
| `test:modes` (разделение режимов на одном сервере) | 29/29 |
| `tests/security/http-suite.mjs` | 52/52 |
| pgTAP (`npm run db:test`) | 213 |
| unit (`npm test`) | 125 |
| `tests/a11y/audit.mjs` | 62/62 |
| `tests/perf/measure.mjs` | 19/28 — см. ниже |

Перф прогонялся против стенда впервые. Его бюджеты писались под localhost, где
страница в миллисекундах; измерение из Казахстана до Токио по нестабильному
каналу с ними не сопоставимо (Today p95 2089ms при бюджете 1500ms). Это не
повод менять бюджеты молча — это повод завести отдельный бюджет для удалённой
цели, если он нужен. Ошибок под нагрузкой ноль, N+1 нет, соединения ограничены.

### Доступы, которые понадобятся

Значений здесь нет и быть не должно — репозиторий публичный. Всё это уже
прописано в переменных окружения Vercel; локально запросите у владельца.

| Что | Где взять |
|---|---|
| `SUPABASE_ACCESS_TOKEN` (`sbp_…`) | https://supabase.com/dashboard/account/tokens |
| Пароль БД каждого проекта | Dashboard → Settings → Database |
| Vercel token | https://vercel.com/account/tokens |
| `GEMINI_API_KEY` | https://aistudio.google.com/apikey |

Переменные окружения стенда: `NEXT_PUBLIC_SUPABASE_URL`,
`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`,
`QADAM_DEMO_TENANTS_ENABLED`, `NEXT_PUBLIC_SITE_URL`, `QADAM_AI_PROVIDER`,
`GEMINI_API_KEY`, `QADAM_AI_MODEL`, `QADAM_JOB_SECRET`, `QADAM_WEBHOOK_SECRET`.

`QADAM_APP_MODE` больше не задаётся: режим стал свойством заведения. Модуль
`src/lib/app-mode.ts` всё ещё читает старую переменную, если новая не задана, —
чтобы нетронутая установка вела себя как раньше.

GitHub: секрет `QADAM_JOB_SECRET` и `VERCEL_TOKEN`, переменные `QADAM_STAND_URL`
и `VERCEL_PROJECT`. Ими живут Deploy и Execution cycle.

---

## 3. Ловушки, на которых легко потерять час

1. **`supabase db push` на CLI 2.110 не работает.** Падает
   `LegacyDbPushApplyError: Connection error` посреди миграции, воспроизводимо,
   и на прямом хосте, и на пулере. Тот же SQL через Management API проходит
   мгновенно — значит дело в CLI, а не в SQL. Старые CLI не понимают текущий
   `config.toml` (`'db' has invalid keys: health_timeout`).
   **Что работает:** скопировать `supabase/migrations` во временную папку с
   `config.toml`, содержащим только `project_id`, и оттуда выполнить
   `npx -y supabase@2.64.1 db push --db-url <URL пулера, порт 5432>`.
   Пароль в URL кодировать (`/` → `%2F`).

2. **Справочные данные кладут миграции, а не seed.** Тарифы, их лимиты,
   `entitlements`, `retention_policies`, `data_inventory`. Локально это
   невидимо, потому что `supabase db reset --local` проигрывает миграции
   вместе с seed. На удалённом стенде возвращается только seed. Если стереть
   эти таблицы, заведение без подписки откатывается на тариф `free`, не находит
   его — и сборка Growth Contract отказывает сообщением
   «Тариф none: 0/0 за период», в котором ничто не указывает на причину.
   `supabase/seed/remote_demo_reset.sql` эти пять таблиц не трогает и в конце
   проверяет, что `free` и его лимит `growth_contracts_month` на месте.

3. **Сеть до `api.supabase.com` и до стендов рвётся.** На машине владельца
   регулярно ловятся `UND_ERR_CONNECT_TIMEOUT` и `ERR_CONNECTION_RESET`.
   `scripts/apply-remote-sql.mjs` и `tests/e2e/remote-query.mjs` уже делают
   повторы. Если пишете новый инструмент, ходящий к Management API, —
   закладывайте повторы сразу, иначе потерянный шаг в паре «сброс, затем seed»
   оставит стенд пустым.

4. **Не гоняйте два набора против одного стенда одновременно.** Они делят одну
   базу, и прогон, который начинается со сброса, снесёт данные под соседним.
   Один раз это уже дало ложное падение.

5. **`private.demo_environment`** — таблица-метка. Пустая на боевом проекте,
   с одной строкой на демонстрационном. Демо-seed и сброс отказываются работать
   на базе без этой строки. Это единственное, что не даёт демо-данным попасть
   в боевую базу, — не удаляйте.

6. **HIBP (защита от утёкших паролей)** у Supabase только на платном тарифе.
   Советник безопасности будет ругаться, включить нельзя.

7. **Планировщик — это GitHub Actions, а не Vercel Cron.**
   `.github/workflows/execution-cycle.yml` вызывает `/api/jobs/run-cycle` каждые
   пять минут. Vercel Cron на бесплатном тарифе ходит раз в сутки и шлёт GET без
   заголовков, а эндпоинт требует POST с секретом. Расписание GitHub —
   best-effort: запуск может опоздать или быть пропущен, и продукт на точный
   такт не полагается.

8. **`service_role` не получает прав автоматически.** Проект создан после смены
   платформенного умолчания Supabase, поэтому новая таблица приходит без грантов
   для него. Миграция `20260802050000` выдаёт их и держит через
   `alter default privileges`; она же падает, если хоть одна таблица в `public`
   осталась нечитаемой. Не отменяйте: без этого админ-клиент молча не видит
   ничего, а цикл выполнения докладывает, что работы нет.

---

## 4. Что было сделано в предыдущей сессии

Всё закоммичено в `master`. Каждое изменение — следствие того, что ломалось
именно на развёрнутом стенде и не могло проявиться локально.

- **Адаптер Gemini** (`src/ai/providers.ts`). `gemini-2.5-flash` новым ключам
  больше не выдают; 3.x отвечает 400 на `thinkingBudget` и требует
  `thinkingLevel`. Форма выбирается по поколению модели из её имени
  (`thinkingConfigFor`), неузнанное имя не шлёт ничего.
- **Метка демо-окружения** — миграция `20260802040000`, таблица
  `private.demo_environment`. Метка уровня базы не подошла: Supabase не даёт
  роли `postgres` определять параметр базы.
- **`scripts/apply-remote-sql.mjs`** — применяет один SQL-файл к удалённому
  проекту через Management API (`db push` умеет только миграции, psql стоит не
  везде).
- **`supabase/seed/remote_demo_seed.sql`** — генерируется из `supabase/seed.sql`
  скриптом `npm run seed:remote`; локальный guard не удаляется, а
  перенаправляется на `private.demo_environment`.
- **`supabase/seed/remote_demo_reset.sql`** — возвращает демо-стенд к seed.
- **Приёмочные наборы научились работать против деплоя:** проверки, читающие
  базу, идут через Management API при заданном `QADAM_SUPABASE_PROJECT_REF`;
  раннер восстанавливает удалённый стенд вместо сброса локальной базы; пороги
  ожиданий масштабируются множителем `PATIENCE` (localhost — 1, удалённая цель
  — 4).
- **Исправление в продукте:** платформенный администратор после входа уходил
  на онбординг, потому что членства в заведении у него нет по устройству.
  Теперь отсутствие членства проверяется на платформенную роль через
  `is_current_platform_admin` (`src/app/auth/actions.ts`).

### Результаты против живого демо-стенда

| Набор | Результат |
|---|---|
| E2E владелец / клиент / администратор | 58/58, 36/36, 45/45 |
| `tests/security/http-suite.mjs` | 43/43 |
| `tests/a11y/audit.mjs` | 62/62 |

Как повторить:

```bash
export SUPABASE_ACCESS_TOKEN=sbp_...
export QADAM_SUPABASE_PROJECT_REF=lwqoclhgdvpydaetpypb
QADAM_E2E_BASE=https://qadam-demo.vercel.app npm run test:e2e
```

Наборы безопасности дополнительно требуют `QADAM_JOB_SECRET` и
`QADAM_WEBHOOK_SECRET` **того стенда**, иначе получите честные 401.

---

## 5. Как устроены режимы

Режим — свойство **заведения**, не установки. `public.businesses.mode`
принимает `demo` или `production`, и база это держит:

```sql
mode text not null default 'production' check (mode in ('demo','production'))
check ((mode='demo' and is_mock) or (mode='production' and not is_mock))
```

плюс триггер `mock rows are forbidden in production businesses` и полтора
десятка security-definer функций, выводящих `is_mock` из этой колонки.

`QADAM_DEMO_TENANTS_ENABLED` — потолок установки: предлагается ли
демонстрационный вход и открыт ли маршрут `/demo`. Никаких поблажек боевому
заведению он не даёт. Режим сужается один раз, в `requireBusinessContext`;
неузнанное значение резолвится в `production`, потому что угадать `demo`
значило бы выдать настоящему арендатору симуляции.

Доказывает это `npm run test:modes`: он регистрирует настоящее заведение на том
же сервере и сравнивает двух арендаторов на одной сборке, а оба отказа базы
проверяет попыткой, а не чтением констрейнта.

Решение и его последствия записаны в [ADR-017](DECISIONS.md).

---

## 6. Что делать дальше

Список задач с проверкой «сделано» для каждой — в [NEXT_TASKS](NEXT_TASKS.md).
Там же записано, что уже закрыто и какими числами это подтверждено.

Прежде чем менять код, повторите прогон против стенда и получите числа из §2.
Если они другие — разбираться нужно с этим, а не писать поверх: значит стенд
или окружение уже не такие, как здесь описано.

### Чего делать нельзя

- Ослаблять констрейнт `(mode='demo' and is_mock) or (mode='production' and not
  is_mock)` или триггер `mock rows are forbidden in production businesses`.
  После объединения режимов именно они держат всю гарантию честности.
- Удалять строку из `private.demo_environment` на демо-проекте или добавлять её
  на боевой.
- Возвращать в воркер молчаливое проглатывание ошибок запроса. «Не смог
  спросить» и «нечего делать» — разные ответы, и один раз это уже стоило
  неработающего цикла выполнения.
- Применять `supabase/seed.sql` где-либо, кроме локальной машины.
- Класть `SUPABASE_SECRET_KEY` в переменную с префиксом `NEXT_PUBLIC_`.
- Коммитить секреты: репозиторий публичный. Ключи — в `CREDENTIALS.local.md`,
  который в `.gitignore`; проверять `git check-ignore -v CREDENTIALS.local.md`.
- Отмечать канал `connected` без пройденной проверки связи — база откажет, и
  это правильно.
