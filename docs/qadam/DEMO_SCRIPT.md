# DEMO SCRIPT — 4 минуты 30 секунд

Сценарий на одиннадцать шагов. Каждый шаг проверяется автоматически
(`node tests/release/demo-script.mjs`, 42/42), так что показывать нужно ровно то, что уже
доказано.

**Где показывать.** https://qadam-growth-os.vercel.app — развёрнутый стенд. На `/login`
кнопка «Войти в DEMO_MODE» открывает кабинет владельца TAMYR сразу, логины вводить не нужно.

**Подготовка.** Свежее окно инкогнито. Перед показом вернуть данные к исходным:
`SUPABASE_ACCESS_TOKEN=sbp_... npm run demo:restore <ref>` — одна команда, чтобы не осталось
следов от прогонов. Локально то же самое даёт `npm run dev:all` и `npx supabase db reset --local`.
Проверено, что весь путь проходит на холодном профиле, при обновлении страницы посреди демо,
при вводе URL вручную, на 390 px и на 1440 px.

**Кадры.** `node scripts/demo-screenshots.mjs` снимает семнадцать экранов со **стенда** в
`docs/qadam/demo/` и падает, если какой-то оказался пустым. Скриншот из презентации — это
утверждение о продукте; эти кадры — его фотография. Два последних снимаются на экране
телефона: карта гостя в Telegram и отказ приложения, открытого вне Telegram.

**Что изменилось и стоит подчеркнуть.** Цифра просадки не записана в базу: её вычисляет
детектор из продаж заведения — те же дни недели, те же часы, неделя к неделе. Поэтому она
слегка меняется от прогона к прогону (около четверти, −22…−27%): это измерение, а не
константа. `npm run demo:restore` прогоняет цикл сразу после seed, чтобы число на экране было
измеренным с самого начала и не менялось посреди показа. История данных всегда заканчивается сегодняшним днём, поэтому демонстрация не устаревает.
Канал — Telegram, и он живой: бот присылает сводку владельцу и принимает подтверждение кнопкой,
а гость вступает в лояльность по QR прямо в чате. WhatsApp не показываем — доступов Meta нет,
и мы их не имитируем.

**Одно правило на всё выступление:** ни одна цифра на экране не выдумана. Любую из них можно
тут же показать в базе. Если спросят — покажите.

---

### 0:00–0:25 · Проблема

> «Кофейня в Алматы. Владелец знает своих гостей в лицо, но не знает, что 64 из них не
> заходили больше месяца. У него нет маркетолога, нет CRM и нет права ошибиться со скидкой:
> у него маржа 42%, и общая скидка 20% съедает её целиком.
>
> Крупная сеть выигрывает масштабом. Локальный бизнес выигрывает отношениями. QADAM
> превращает эти отношения в измеримый рост — и не даёт потерять на этом деньги.»

Открыть `/` — заголовок «Самое выгодное действие — сегодня».

### 0:25–0:45 · Шаг 1. Вход

Нажать **«Войти в DEMO_MODE»**.

> «Это демонстрационный режим. Всё, что вы увидите помеченным DEMO DATA, — синтетическое. Я
> буду говорить об этом каждый раз, потому что продукт устроен так, чтобы никогда не выдать
> смоделированный результат за факт.»

### 0:45–1:00 · Шаг 2. Онбординг

> «Заведение заводится за шесть шагов. Прогресс хранится на сервере — закрыли вкладку,
> вернулись, продолжили с того же места.»

### 1:00–1:25 · Шаг 3. Today: провал в тихие часы

Открыть **Сегодня**.

> «Первый экран не показывает двадцать графиков. Он показывает одну вещь: в будни с 15 до 18
> выручка ниже сопоставимого периода примерно на четверть — точное число на экране посчитано
> из продаж этого заведения минуту назад. Ниже — источник, период и с чем
> сравнивали. QADAM показывает гипотезу причины и прямо пишет, что это гипотеза, а не
> утверждение.»

Цифра приходит из `signals.change_bps`, и её туда записал детектор, а не seed.

### 1:25–1:50 · Шаг 4. 64 → 18

Открыть Campaign Studio, шаг «Аудитория».

> «Спящих клиентов 64. Но написать можно восемнадцати. Разница — согласие: у остальных его
> нет или оно отозвано. Это не фильтр в интерфейсе, это правило в базе. Даже если попытаться
> отправить остальным, база не даст.»

Обе цифры и объяснение — на одном экране.

### 1:50–2:20 · Шаг 5. Margin Shield отказывает

В истории кампаний уже лежит отклонённый вариант — откройте его перед тем, как собирать свой.

> «Вот вариант, который система запретила неделю назад: скидка 20% на весь заказ. Она
> опускает вклад-маржу до 22% при пороге владельца 42% — минус 2 600 тенге в базовом
> сценарии. Рядом две безопасные альтернативы, которые сервер предложил взамен.
>
> И заметьте статус: черновик. Заблокированный вариант физически не может стать
> утверждённым — база отказывается принять контракт выше черновика, если Margin Shield его
> не пропустил. Это не проверка в интерфейсе.»

Теперь то же вживую.

Механика «Скидка в процентах», размер 20%, без порога.

> «Владелец хочет дать 20 процентов. Смотрите: Margin Shield запрещает запуск. Не
> предупреждает — запрещает. Из заблокированного варианта нельзя собрать контракт. И считает
> это не модель, а сервер: языковая модель здесь не имеет права голоса.»

### 2:20–2:45 · Шаг 6. Безопасная альтернатива

Механика «Подарок при пороге», круассан при чеке от 3 500 ₸.

> «А вот это проходит. Разница — порог: подарок отдаётся только при чеке выше среднего,
> поэтому маржа остаётся. Симулятор показывает низкий, базовый и высокий сценарий, стоимость,
> каннибализацию и ROI — до запуска, а не после.»

### 2:45–3:10 · Шаг 7. Growth Contract и два языка

Собрать контракт, открыть Контент-студию.

> «Это Growth Contract: аудитория, оффер, лимиты, правило остановки и способ измерения — в
> одном пакете. При подтверждении он замораживается: снимок и хеш. Что бы дальше ни поменяли
> в шаблонах, этот контракт останется таким, каким его подтвердили.
>
> Тексты сразу на русском и казахском. И заметьте пометку: казахский вариант помечен как
> требующий проверки носителем языка. Мы не делаем вид, что машинный казахский готов к
> публикации.»

### 3:10–3:30 · Шаг 8. Запуск и история

> «Запускаем. В демо-режиме это симуляция — так и написано. Ни одно сообщение никуда не
> ушло: провайдер не подключён, и продукт говорит об этом прямо, вместо того чтобы показать
> зелёную галочку. В журнале осталась запись: кто, когда, что именно.»

### 3:30–4:00 · Шаг 9. Impact Ledger

Открыть **Аналитику**.

> «Здесь главное. 103 500 тенге — выручка тех, кто получил сообщение. Это *влияние*, и это
> не заслуга кампании: часть из них пришла бы и так.
>
> Прирост — отдельная строка: 48 700 тенге, и она помечена как оценка. Разделять эти два
> числа — то, чего почти никто не делает, и именно поэтому владельцы перестают верить
> отчётам.
>
> Ни одна строка здесь не названа проверенным фактом. Проверенный факт появится только тогда,
> когда будет подключён реальный источник данных.»

### 4:00–4:20 · Шаг 10. Карта гостя в Telegram

Открыть [@qadam_serpin_bot](https://t.me/qadam_serpin_bot), нажать кнопку **«Открыть мою карту»**.

> «А это то же заведение глазами гостя, внутри Telegram. Штампы, сколько осталось до
> бесплатного круассана, свои визиты, меню и действующие предложения. Награду он забирает
> отсюда же — и это списание с теми же проверками, что на кассе.
>
> Обратите внимание на две вещи. Первая: согласие на рассылку он может отозвать одной
> кнопкой, и это подействует немедленно. Вторая: полного номера телефона у заведения нет —
> контакт хранится хешем и маской, для полного значения в базе нет колонки.
>
> Владелец в том же приложении видит сигнал дня и подтверждает запуск. Тот же путь, те же
> проверки Margin Shield — просто на телефоне.»

Открыть приложение по прямой ссылке вне Telegram — оно откажет и объяснит почему.

> «И его нельзя открыть просто так. Telegram подписывает данные о том, кто открыл
> приложение; без подписи не открывается ничего. Подделанная подпись — отказ.»

### 4:20–4:30 · Шаг 11. Админка

Войти как `admin@qadam.local`, открыть **Шаблоны**.

> «И платформенная часть. Шаблон клонируется, версионируется, публикуется. Опубликованную
> версию изменить нельзя — база откажет. Откат переключает шаблон, но не стирает историю.
> Каждое действие администратора записано с причиной и не может быть отредактировано задним
> числом.
>
> Заведение владельца при этом не трогается: у его контракта свой замороженный снимок.»

---

## Если спросят

**«Это реальные данные?»** Нет. Синтетическое заведение TAMYR Coffee: 180 клиентов, 1 129
транзакций за 120 дней. Каждая строка помечена в базе. Могу показать пометку.

**«А AI здесь настоящий?»** Да, и видно где именно. Механики акций и тексты пишет Gemini —
каждый прогон записан в журнал генераций с моделью, стоимостью и результатом проверок
безопасности. Но модель не считает деньги: экономику, симулятор и Margin Shield считает
сервер, а ответ модели проверяется по схеме и отклоняется целиком, если в нём чего-то не
хватает. Досье гостя устроено ещё строже: число, которого нет в его собственных данных, в
досье попасть не может — проверка отклонит весь ответ. Когда провайдер недоступен, работает
встроенный шаблон, и на карточке написано, что это шаблон.

**«Сообщения правда отправляются?»** Telegram — да, по-настоящему: бот присылает владельцу
сводку дня, принимает подтверждение кнопкой, гость вступает в программу лояльности прямо в
чате и открывает там свою карту. Остальные каналы не подключены, и продукт не даёт каналу считаться подключённым, пока не
пройдёт настоящая проверка связи.

Но обратите внимание на разницу. Сообщение уходит по-настоящему — а вот **выручка по нему
остаётся демонстрационной**: демо-заведение получает состояние `sandbox`, и «проверенным
фактом» результат здесь не станет. Отправка и утверждение о деньгах — разные вещи, и продукт
их не смешивает.

**«А если я всё-таки хочу свои 20 процентов?»** Margin Shield не обходится из интерфейса.
Можно поднять порог, сузить аудиторию, изменить механику — тогда сервер пересчитает. Но
записать заблокированное решение как одобренное нельзя: база откажет.

**«Что с законом о персональных данных?»** Контакт хранится как необратимый хеш и маска —
полного адреса в базе нет вообще, для него нет колонки. Согласия раздельные: участие в
программе и маркетинг. Отзыв сохраняется как доказательство. **Юрист это не проверял**, и об
этом написано на самой странице приватности.

**«Сколько пользователей выдержит?»** Не знаю, и никто не знает: замеры сделаны на одном
локальном процессе. Могу показать конкретные цифры того замера, но выводов о ёмкости
платформы из них не следует.

**«Что здесь ещё не готово?»** Платёжный провайдер, каналы отправки, юридическая проверка,
ревью казахского носителем и развёртывание. Всё перечислено в KNOWN_LIMITATIONS — не в
сносках, а списком.
